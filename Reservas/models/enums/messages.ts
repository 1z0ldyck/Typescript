export enum Messages {
  CHECKOUT_MUST_BE_AFTER = "Check-out date must be after check-in date",
  RESERVATION_MUST_BE_FUTURE_DATES = "Reservation dates for update must be future dates"
}

export default Messages;